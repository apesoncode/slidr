RSpec.describe Slidr do
  it "has a version number" do
    expect(Slidr::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(false)
    expect(false).to be_false
  end

end
